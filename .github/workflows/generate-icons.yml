##########################
#
# Generate macOS (.icns), Windows (.ico), and SDL window icon header
# from SVG sources for all three app variants plus the launcher.
#
# Runs on macos-15 (required for iconutil).
# Commits generated icons back to main, then
# propagates the changes to all other branches.
#
##########################
name: "Generate macOS and Windows Icons"

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'misc/openmohaa.svg'
      - 'misc/openmohaab.svg'
      - 'misc/openmohaas.svg'
      - 'misc/launcher.svg'

jobs:
  generate-icons:
<<<<<<< HEAD
    name: "Generate .icns, .ico, and SDL header"
=======
    name: "Generate .icns and .ico files"
>>>>>>> origin/main
    runs-on: macos-15
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install tools
      run: brew install librsvg imagemagick

    - name: Generate icons
      run: |
<<<<<<< HEAD
        set -euo pipefail

=======
>>>>>>> origin/main
        for app in openmohaa openmohaab openmohaas launcher; do
          SVG="misc/${app}.svg"

          ###
          # macOS .icns
          ###
          mkdir -p "${app}.iconset"
          rsvg-convert -w 16   -h 16   "$SVG" -o "${app}.iconset/icon_16x16.png"
          rsvg-convert -w 32   -h 32   "$SVG" -o "${app}.iconset/icon_16x16@2x.png"
          rsvg-convert -w 32   -h 32   "$SVG" -o "${app}.iconset/icon_32x32.png"
          rsvg-convert -w 64   -h 64   "$SVG" -o "${app}.iconset/icon_32x32@2x.png"
          rsvg-convert -w 128  -h 128  "$SVG" -o "${app}.iconset/icon_128x128.png"
          rsvg-convert -w 256  -h 256  "$SVG" -o "${app}.iconset/icon_128x128@2x.png"
          rsvg-convert -w 256  -h 256  "$SVG" -o "${app}.iconset/icon_256x256.png"
          rsvg-convert -w 512  -h 512  "$SVG" -o "${app}.iconset/icon_256x256@2x.png"
          rsvg-convert -w 512  -h 512  "$SVG" -o "${app}.iconset/icon_512x512.png"
          rsvg-convert -w 1024 -h 1024 "$SVG" -o "${app}.iconset/icon_512x512@2x.png"
          iconutil -c icns "${app}.iconset" -o "misc/macos/${app}.icns"
          rm -rf "${app}.iconset"

          ###
          # Windows .ico
          ###
<<<<<<< HEAD
          for size in 16 32 48 256; do
            rsvg-convert -w $size -h $size "$SVG" -o "${app}_${size}.png"
          done
          magick "${app}_16.png" "${app}_32.png" "${app}_48.png" "${app}_256.png" \
                 "misc/windows/${app}.ico"
          rm -f "${app}_16.png" "${app}_32.png" "${app}_48.png" "${app}_256.png"
        done

    - name: Generate SDL window icon header
      run: |
        set -euo pipefail
        tmpdir=$(mktemp -d)
        rsvg-convert -w 32 -h 32 misc/openmohaa.svg -o "$tmpdir/icon32.png"
        magick "$tmpdir/icon32.png" -depth 8 "RGBA:$tmpdir/icon32.rgba"
        python3 -c "
        with open('$tmpdir/icon32.rgba', 'rb') as f:
            data = f.read()
        assert len(data) == 32 * 32 * 4, f'Expected {32*32*4} bytes, got {len(data)}'
        lines = []
        lines.append('/* Generated from misc/openmohaa.svg - do not edit manually */')
        lines.append('')
        lines.append('static const struct {')
        lines.append('  unsigned int \t width;')
        lines.append('  unsigned int \t height;')
        lines.append('  unsigned int \t bytes_per_pixel; /* 2:RGB16, 3:RGB, 4:RGBA */ ')
        lines.append('  unsigned char\t pixel_data[32 * 32 * 4 + 1];')
        lines.append('} CLIENT_WINDOW_ICON = {')
        lines.append('  32, 32, 4,')
        buf = '  \"'
        col = 3
        for byte in data:
            if byte >= 32 and byte < 127 and byte != ord(chr(34)) and byte != ord(chr(92)):
                esc = chr(byte)
                esc_len = 1
            else:
                esc = '\\\\{:03o}'.format(byte)
                esc_len = 4
            if col + esc_len > 74:
                buf += '\"\n  \"'
                col = 3
            buf += esc
            col += esc_len
        buf += '\",'
        lines.append(buf)
        lines.append('};')
        lines.append('')
        with open('code/sdl/sdl_icon.h', 'w', newline='\n') as f:
            f.write('\n'.join(lines))
        "
        rm -rf "$tmpdir"

    - name: Commit and push icons to all branches
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

        git add misc/macos/openmohaa.icns misc/macos/openmohaab.icns misc/macos/openmohaas.icns misc/macos/launcher.icns
        git add misc/windows/openmohaa.ico misc/windows/openmohaab.ico misc/windows/openmohaas.ico misc/windows/launcher.ico
        git add code/sdl/sdl_icon.h
=======
          for size in 16 32 48 64 128 256; do
            rsvg-convert -w $size -h $size "$SVG" -o "${app}_${size}.png"
          done
          magick "${app}_16.png" "${app}_32.png" "${app}_48.png" \
                 "${app}_64.png" "${app}_128.png" "${app}_256.png" \
                 "misc/windows/${app}.ico"
          rm -f "${app}_16.png" "${app}_32.png" "${app}_48.png" \
                "${app}_64.png" "${app}_128.png" "${app}_256.png"
        done

    - name: Commit and push icons to all branches
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

        git add misc/macos/openmohaa.icns misc/macos/openmohaab.icns misc/macos/openmohaas.icns misc/macos/launcher.icns
        git add misc/windows/openmohaa.ico misc/windows/openmohaab.ico misc/windows/openmohaas.ico misc/windows/launcher.ico
>>>>>>> origin/main

        if git diff --staged --quiet; then
          echo "No icon changes detected."
          exit 0
        fi

<<<<<<< HEAD
        git commit -m "Update macOS .icns, Windows .ico, and SDL icon header from SVG sources"
=======
        git commit -m "Update macOS .icns and Windows .ico icons from SVG sources"
>>>>>>> origin/main
        git push origin main

        # Propagate generated icons to all other remote branches
        for branch in $(git branch -r | grep -v 'HEAD' | grep -v 'origin/main' | sed 's|origin/||'); do
          echo "Updating branch: $branch"
          git checkout "$branch"
          git checkout main -- \
            misc/macos/openmohaa.icns misc/macos/openmohaab.icns misc/macos/openmohaas.icns misc/macos/launcher.icns \
<<<<<<< HEAD
            misc/windows/openmohaa.ico misc/windows/openmohaab.ico misc/windows/openmohaas.ico misc/windows/launcher.ico \
            code/sdl/sdl_icon.h
          if ! git diff --staged --quiet; then
            git commit -m "Update macOS .icns, Windows .ico, and SDL icon header from SVG sources"
=======
            misc/windows/openmohaa.ico misc/windows/openmohaab.ico misc/windows/openmohaas.ico misc/windows/launcher.ico
          if ! git diff --staged --quiet; then
            git commit -m "Update macOS .icns and Windows .ico icons from SVG sources"
>>>>>>> origin/main
            git push origin "$branch"
          fi
        done
